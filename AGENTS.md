# AGENTS

## Development
- Install dependencies: `npm install`.
- Start dev server: `npm run dev`.

## Validation
- Lint (quality gate): `npm run lint`.
- Typecheck JS/JSX (quality gate): `npm run typecheck`.
- Build (quality gate): `npm run build`.

Note: Quality-gate commands mirror the CI expectations for this repo.
